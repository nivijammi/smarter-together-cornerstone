package com.kenzie.appserver.controller.model;

public enum RegistrationStatus {
    LOGIN_SUCCESSFUL,
    LOGIN_UNSUCCESSFUL,
    EMAIL_EXISTS,
    EMAIL_INVALID,
    PASSWORD_NOT_MATCHED,
    USER_NOT_FOUND,

    USER_REGISTERED
}
